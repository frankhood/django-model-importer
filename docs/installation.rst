============
Installation
============

At the command line::

    $ easy_install django-model-importer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-model-importer
    $ pip install django-model-importer
